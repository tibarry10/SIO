<Window x:Class="Projet_refaire.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Projet_refaire"
        mc:Ignorable="d"
        Title="Jeu Devine Nombre" Height="450" Width="800">
    <Grid>
        <Grid.ColumnDefinitions/>

        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="Devinez un nombre entre 1 et 100" FontSize="20" Margin="10" HorizontalAlignment="Center" />
        <TextBlock Grid.Row="1" Text="Tentatives restantes: " FontSize="20" Margin="10" HorizontalAlignment="Center" />
        <TextBlock Grid.Row="2" Text="{Binding TentativeRestantes}" FontSize="20" Margin="10" HorizontalAlignment="Center" Foreground="Blue" />
        <TextBox Grid.Row="3" x:Name="myTextBox" Text="{Binding Proposition, UpdateSourceTrigger=PropertyChanged}" FontSize="20" Margin="10" Width="70" HorizontalAlignment="Center" TextAlignment="Center" />
        <Button Content="Ok" Grid.Row="4" Command="{Binding OkCommand}" FontSize="14" Margin="10" HorizontalAlignment="Center" Width="70" />
        <Button Content="Rejouer" Grid.Row="5" Command="{Binding RejouerCommand}" FontSize="16" FontWeight="Bold" Margin="10" Width="70" VerticalAlignment="Center" Foreground="{Binding Couleur}" />
        <TextBlock Grid.Row="6" Text="{Binding Reponse}" FontSize="20" Margin="10" HorizontalAlignment="Center" TextAlignment="Center" />
        <Image Grid.Row="7" Source="{Binding ImageSource}" Height="100" HorizontalAlignment="Center" />

    </Grid>
</Window>
