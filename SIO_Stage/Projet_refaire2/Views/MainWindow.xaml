<Window x:Class="Projet_refaire2.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Projet_refaire2"
        mc:Ignorable="d"
        Title="MainWindow" Height="450" Width="800">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition/>
        </Grid.ColumnDefinitions>

        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="Devinez un nombre entre 1 et 99" />
        <TextBlock Grid.Row="1" Text="Tentatives Restantes:" />
        <TextBlock Grid.Row="2" Text="{Binding TentativesRestantes}" />
        <TextBox x:Name="myTextBox" Grid.Row="3" Text="{Binding Proposition, UpdateSourceTrigger=PropertyChanged}"/>
        <Button Grid.Row="4" x:Name="BtnOK" Content="OK" Command="{Binding OkCommand}" IsDefault="{Binding DefaultOk}"/>
        <Button Grid.Row="5" x:Name="BtnRejouer" Content="Rejouer" Command="{Binding RejouerCommand}" IsDefault="{Binding DefaultRejouer}"/>
        <TextBlock Grid.Row="6" Text="{Binding Reponse}" />
        <Image Source="{Binding ImageSource}" />

    </Grid>
</Window>
