<Window x:Class="Chifoumi.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Chifoumi"
        mc:Ignorable="d"
        Title="MainWindow" Height="450" Width="800">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition />
            <ColumnDefinition />
            <ColumnDefinition />
            <ColumnDefinition />
        </Grid.ColumnDefinitions>

        <Grid.RowDefinitions>
            <RowDefinition Height = "Auto" />
            <RowDefinition Height = "Auto" />
        </Grid.RowDefinitions>
        <Image Grid.Column="0" Grid.Row="0" Source="{Binding ImageSource}" />
        <Image Grid.Column="1" Grid.Row="0" Source="{Binding ImageSource}" />
        <Image Grid.Column="2" Grid.Row="0" Source="{Binding ImageSource}" />

        <Button Grid.Column="2" Grid.Row="3" x:Name="BtnPierre" Content="Pierre" Command="{Binding PierreCommand}" />
        <Button Grid.Column="2" Grid.Row="4" x:Name="BtnFeuille" Content="Feuille" Command="{Binding FeuilleCommand}" />
        <Button Grid.Column="2" Grid.Row="5" x:Name="BtnCiseau" Content="Ciseau" Command="{Binding CiseauCommand}" />
        <TextBlock Grid.Row="3" Text="{Binding Score}" TextAlignment="Center" />
        <TextBlock Grid.Row="6" Text="{Binding Reponse}" />



    </Grid>
</Window>
